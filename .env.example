# Authentication
GOOGLE_ID=YOUR_GOOGLE_ID # ...apps.googleusercontent.com format
GOOGLE_SECRET=YOUR_GOOGLE_SECRET
NEXTAUTH_URL=http://localhost:3000
# Should Change to production URL

# Stripe
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=

# Stripe Terminal/CLI
STRIPE_SIGNING_SECRET=

HOST=http://localhost:3000

NEXTAUTH_SECRET=YOUR_SECRET_KEY
# only requied for production. Can be generated by using command following on terminal "openssl rand -base64 32"

# On the google cloud, add this URL for defining callback
# http://localhost:3000/api/auth/callback/google
# On production, http://localhost:3000 will change to production URL